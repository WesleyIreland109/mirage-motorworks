import { FormEvent, useState } from "react";
import { Link } from "react-router-dom";
import { forgotPassword } from "@/api/client";
import { Logo } from "@/components/Logo"; import { Button } from "@/components/ui/button"; import { Card } from "@/components/ui/card"; import { Input } from "@/components/ui/input";

export function ForgotPasswordPage(){const[email,setEmail]=useState("");const[busy,setBusy]=useState(false);const[sent,setSent]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{await forgotPassword(email);setSent(true)}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-mirage-bg px-5 text-white"><Card className="w-full max-w-md p-7"><Logo to="/"/><p className="mt-10 text-xs font-semibold uppercase tracking-[.24em] text-mirage-cyan">Account recovery</p><h1 className="mt-2 text-3xl font-semibold">Reset your password</h1>{sent?<div className="mt-6"><p className="text-sm leading-6 text-mirage-muted">If an account exists for that email, a reset link is on its way. Check spam if it does not arrive shortly.</p><Link className="mt-6 inline-block text-sm text-mirage-cyan" to="/login">Return to sign in</Link></div>:<form className="mt-8 space-y-5" onSubmit={submit}><label className="block text-sm font-medium">Email<Input className="mt-2" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><Button className="w-full" disabled={busy}>{busy?"Sending…":"Send reset link"}</Button><p className="text-center text-sm"><Link className="text-mirage-cyan" to="/login">Return to sign in</Link></p></form>}</Card></main>}
