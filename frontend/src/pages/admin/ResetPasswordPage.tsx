import { FormEvent, useState } from "react";
import { Link, useSearchParams } from "react-router-dom";
import { resetPassword } from "@/api/client";
import { Logo } from "@/components/Logo"; import { Button } from "@/components/ui/button"; import { Card } from "@/components/ui/card"; import { Input } from "@/components/ui/input";

export function ResetPasswordPage(){const[params]=useSearchParams();const token=params.get("token")??"";const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[error,setError]=useState("");const[done,setDone]=useState(false);const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");if(password!==confirm){setError("The passwords do not match.");return}setBusy(true);try{await resetPassword(token,password);setDone(true)}catch{setError("The link is invalid or expired, or the password does not meet the requirements.")}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-mirage-bg px-5 text-white"><Card className="w-full max-w-md p-7"><Logo to="/"/><p className="mt-10 text-xs font-semibold uppercase tracking-[.24em] text-mirage-cyan">Account recovery</p><h1 className="mt-2 text-3xl font-semibold">Choose a new password</h1>{done?<div className="mt-6"><p className="text-sm text-emerald-300">Your password has been updated and existing sessions were signed out.</p><Link className="mt-6 inline-block text-sm text-mirage-cyan" to="/login">Sign in</Link></div>:<form className="mt-8 space-y-5" onSubmit={submit}><p className="text-sm leading-6 text-mirage-muted">Use 12–128 characters with uppercase, lowercase, and a number. Symbols such as !@#$%^&amp;* are supported.</p><label className="block text-sm font-medium">New password<Input className="mt-2" type="password" autoComplete="new-password" minLength={12} maxLength={128} required value={password} onChange={e=>setPassword(e.target.value)}/></label><label className="block text-sm font-medium">Confirm password<Input className="mt-2" type="password" autoComplete="new-password" required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<p className="text-sm text-mirage-orange">{error}</p>}<Button className="w-full" disabled={busy||!token}>{busy?"Updating…":"Update password"}</Button>{!token&&<p className="text-sm text-mirage-orange">This reset link is missing its token.</p>}</form>}</Card></main>}
